workflows:
  expo-android-build:
    name: Expo Android Build
    max_build_duration: 30
    environment:
      node: 18
    scripts:
      - npm install
      - npx expo prebuild
      - cd android
      - ./gradlew assembleRelease
    artifacts:
      - android/app/build/outputs/**/*.apk
